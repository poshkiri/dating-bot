# 🤖 Управление ботом

Полное руководство по управлению ботом: запуск, остановка, перезагрузка и другие операции.

## 📋 Содержание

1. [Запуск бота](#запуск-бота)
2. [Остановка бота](#остановка-бота)
3. [Перезагрузка бота](#перезагрузка-бота)
4. [Проверка статуса](#проверка-статуса)
5. [Просмотр логов](#просмотр-логов)
6. [Автозапуск при перезагрузке](#автозапуск-при-перезагрузке)
7. [Обновление кода](#обновление-кода)
8. [Резервное копирование](#резервное-копирование)
9. [Мониторинг](#мониторинг)

---

## 🚀 Запуск бота

### Локально (Windows):

```bash
cd dating-bot
python main.py
```

### Локально (Linux/Mac):

```bash
cd dating-bot
python3 main.py
```

### В фоновом режиме (Linux/Mac):

#### Вариант 1: Используя nohup

```bash
cd dating-bot
nohup python3 main.py > bot.log 2>&1 &
```

Бот запустится в фоне, логи будут в файле `bot.log`.

#### Вариант 2: Используя screen

```bash
# Установите screen (если нет)
sudo apt install screen  # Ubuntu/Debian
brew install screen      # macOS

# Запустите бота
cd dating-bot
screen -S bot
python3 main.py

# Отсоединитесь от screen (бот продолжит работать)
# Нажмите: Ctrl+A, затем D
```

**Вернуться к боту:**
```bash
screen -r bot
```

#### Вариант 3: Используя tmux

```bash
# Установите tmux (если нет)
sudo apt install tmux    # Ubuntu/Debian
brew install tmux        # macOS

# Запустите бота
cd dating-bot
tmux new -s bot
python3 main.py

# Отсоединитесь от tmux (бот продолжит работать)
# Нажмите: Ctrl+B, затем D
```

**Вернуться к боту:**
```bash
tmux attach -t bot
```

### На сервере (systemd service):

```bash
# Запустить
sudo systemctl start dating-bot

# Проверить статус
sudo systemctl status dating-bot

# Включить автозапуск
sudo systemctl enable dating-bot
```

---

## 🛑 Остановка бота

### Если бот запущен в терминале:

Нажмите `Ctrl+C` в терминале, где запущен бот.

### Если бот запущен в фоне (nohup):

```bash
# Найти процесс
ps aux | grep "python.*main.py"

# Остановить по PID (замените 12345 на реальный PID)
kill 12345

# Или найти и остановить одной командой
pkill -f "python.*main.py"
```

### Если бот запущен в screen:

```bash
# Подключиться к screen
screen -r bot

# Нажать Ctrl+C для остановки

# Или убить screen сессию
screen -X -S bot quit
```

### Если бот запущен в tmux:

```bash
# Подключиться к tmux
tmux attach -t bot

# Нажать Ctrl+C для остановки

# Или убить tmux сессию
tmux kill-session -t bot
```

### Если бот запущен как systemd service:

```bash
# Остановить
sudo systemctl stop dating-bot

# Проверить статус
sudo systemctl status dating-bot
```

---

## 🔄 Перезагрузка бота

### Быстрая перезагрузка:

1. **Остановите бота** (см. раздел "Остановка бота")
2. **Запустите бота снова** (см. раздел "Запуск бота")

### Перезагрузка systemd service:

```bash
# Перезагрузить
sudo systemctl restart dating-bot

# Или остановить и запустить
sudo systemctl stop dating-bot
sudo systemctl start dating-bot
```

### Перезагрузка с обновлением кода:

```bash
# 1. Остановите бота
sudo systemctl stop dating-bot

# 2. Обновите код (если используете Git)
cd dating-bot
git pull

# 3. Установите новые зависимости (если есть)
pip3 install -r requirements.txt

# 4. Запустите бота
sudo systemctl start dating-bot
```

---

## ✅ Проверка статуса

### Проверить, запущен ли бот:

#### Linux/Mac:

```bash
# Проверить процессы
ps aux | grep "python.*main.py"

# Или более точно
pgrep -f "python.*main.py"
```

#### Windows:

```powershell
# Проверить процессы
tasklist | findstr python
```

#### Проверить systemd service:

```bash
sudo systemctl status dating-bot
```

**Статусы:**
- `active (running)` - бот работает ✅
- `inactive (dead)` - бот остановлен ❌
- `failed` - ошибка запуска ⚠️

### Проверить, отвечает ли бот:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Если бот отвечает - он работает ✅

---

## 📝 Просмотр логов

### Если бот запущен в терминале:

Логи выводятся прямо в терминал.

### Если бот запущен через nohup:

```bash
# Просмотр логов в реальном времени
tail -f dating-bot/bot.log

# Последние 100 строк
tail -n 100 dating-bot/bot.log

# Поиск ошибок
grep -i error dating-bot/bot.log
```

### Если бот запущен через screen/tmux:

```bash
# Подключиться и увидеть логи
screen -r bot
# или
tmux attach -t bot
```

### Если бот запущен как systemd service:

```bash
# Просмотр логов
sudo journalctl -u dating-bot -f

# Последние 100 строк
sudo journalctl -u dating-bot -n 100

# Логи за сегодня
sudo journalctl -u dating-bot --since today

# Поиск ошибок
sudo journalctl -u dating-bot | grep -i error
```

### Сохранение логов в файл:

Добавьте в `main.py`:

```python
import logging

# Настройка логирования в файл
file_handler = logging.FileHandler('bot.log')
file_handler.setLevel(logging.INFO)
formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')
file_handler.setFormatter(formatter)
logger.addHandler(file_handler)
```

---

## 🔄 Автозапуск при перезагрузке

### Вариант 1: systemd service (Linux) ⭐

Создайте файл `/etc/systemd/system/dating-bot.service`:

```ini
[Unit]
Description=Dating Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/dating-bot
ExecStart=/usr/bin/python3 /root/dating-bot/main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Активация:**

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable dating-bot

# Запустить сервис
sudo systemctl start dating-bot

# Проверить статус
sudo systemctl status dating-bot
```

### Вариант 2: crontab (Linux/Mac)

```bash
# Открыть crontab
crontab -e

# Добавить строку (замените путь на ваш)
@reboot cd /path/to/dating-bot && /usr/bin/python3 main.py >> bot.log 2>&1
```

### Вариант 3: Windows Task Scheduler

1. Откройте "Планировщик заданий"
2. Создайте новое задание
3. Триггер: "При запуске компьютера"
4. Действие: Запустить программу
   - Программа: `python`
   - Аргументы: `C:\path\to\dating-bot\main.py`
   - Рабочая папка: `C:\path\to\dating-bot`

---

## 📦 Обновление кода

### Если используете Git:

```bash
# 1. Остановите бота
sudo systemctl stop dating-bot

# 2. Обновите код
cd dating-bot
git pull

# 3. Установите новые зависимости (если requirements.txt изменился)
pip3 install -r requirements.txt

# 4. Примените миграции БД (если есть)
alembic upgrade head

# 5. Запустите бота
sudo systemctl start dating-bot

# 6. Проверьте логи
sudo journalctl -u dating-bot -f
```

### Если не используете Git:

1. Остановите бота
2. Загрузите новый код вручную
3. Установите зависимости
4. Запустите бота

---

## 💾 Резервное копирование

### Резервное копирование базы данных:

#### SQLite:

```bash
# Создать бэкап
cp dating-bot/test_bot.db dating-bot/backup_$(date +%Y%m%d_%H%M%S).db

# Или с остановкой бота (рекомендуется)
sudo systemctl stop dating-bot
cp dating-bot/test_bot.db dating-bot/backup_$(date +%Y%m%d_%H%M%S).db
sudo systemctl start dating-bot
```

#### MongoDB:

```bash
# Создать бэкап
mongodump --uri="mongodb://localhost:27017" --db=dating_bot --out=/backup/$(date +%Y%m%d)

# Восстановить из бэкапа
mongorestore --uri="mongodb://localhost:27017" /backup/20240101/dating_bot
```

#### MongoDB Atlas:

MongoDB Atlas делает автоматические бэкапы. Вы можете:
1. Зайти в MongoDB Atlas
2. Перейти в "Backup"
3. Создать snapshot вручную или восстановить из существующего

### Резервное копирование .env файла:

```bash
# Создать бэкап
cp dating-bot/.env dating-bot/.env.backup_$(date +%Y%m%d)
```

### Автоматическое резервное копирование (cron):

```bash
# Открыть crontab
crontab -e

# Добавить (каждый день в 3:00)
0 3 * * * cp /path/to/dating-bot/test_bot.db /backup/db_$(date +\%Y\%m\%d).db
```

---

## 📊 Мониторинг

### Проверка использования ресурсов:

```bash
# Использование CPU и памяти
top -p $(pgrep -f "python.*main.py")

# Или используя htop
htop
```

### Проверка дискового пространства:

```bash
# Размер базы данных
du -h dating-bot/test_bot.db

# Размер логов
du -h dating-bot/bot.log

# Общее использование диска
df -h
```

### Мониторинг через systemd:

```bash
# Статус и использование ресурсов
systemctl status dating-bot

# Детальная информация
systemctl show dating-bot
```

### Внешний мониторинг:

Используйте сервисы для мониторинга:
- [UptimeRobot](https://uptimerobot.com) - бесплатный мониторинг
- [Pingdom](https://www.pingdom.com) - платный
- [StatusCake](https://www.statuscake.com) - бесплатный тариф

Настройте проверку через Telegram бота (отправка `/start` каждые 5 минут).

---

## 🔧 Полезные команды

### Найти и убить процесс бота:

```bash
# Найти PID
ps aux | grep "python.*main.py" | grep -v grep

# Убить процесс
pkill -f "python.*main.py"

# Или принудительно
pkill -9 -f "python.*main.py"
```

### Очистка логов:

```bash
# Очистить файл логов
> dating-bot/bot.log

# Или удалить старые логи
find dating-bot -name "*.log" -mtime +30 -delete
```

### Проверка зависимостей:

```bash
# Проверить установленные пакеты
pip3 list | grep -E "aiogram|sqlalchemy|motor"

# Обновить все зависимости
pip3 install --upgrade -r requirements.txt
```

### Проверка подключений:

```bash
# Проверить подключение к базе данных
# SQLite - файл должен существовать
ls -lh dating-bot/test_bot.db

# MongoDB - проверить подключение
mongosh "mongodb://localhost:27017"
```

---

## ❓ Часто задаваемые вопросы

### Бот не запускается

1. Проверьте логи: `sudo journalctl -u dating-bot -n 50`
2. Проверьте .env файл: `cat dating-bot/.env`
3. Проверьте зависимости: `pip3 install -r requirements.txt`
4. Проверьте права доступа: `ls -la dating-bot/`

### Бот запускается, но не отвечает

1. Проверьте токен бота в .env
2. Проверьте, что бот не заблокирован в Telegram
3. Отправьте `/start` боту
4. Проверьте логи на ошибки

### Бот падает с ошибкой

1. Просмотрите логи: `sudo journalctl -u dating-bot -f`
2. Проверьте базу данных
3. Проверьте подключение к интернету
4. Проверьте все настройки в .env

### Как перезапустить бота после изменения кода?

```bash
sudo systemctl restart dating-bot
```

### Как посмотреть последние ошибки?

```bash
sudo journalctl -u dating-bot | grep -i error | tail -20
```

---

## 📚 Дополнительные ресурсы

- Основная документация: `../README.md`
- Настройка: `../env.example`
- Структура проекта: см. README.md

---

**Успешного управления ботом! 🚀**

