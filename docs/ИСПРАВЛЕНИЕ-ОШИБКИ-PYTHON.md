# 🔧 Исправление ошибки сборки на Railway

## ❌ Проблема

При развертывании на Railway возникает ошибка:
```
ОШИБКА: Не удалось собрать wheel для asyncpg
```

**Причина:** `asyncpg` несовместим с Python 3.13, который использует Railway по умолчанию.

## ✅ Решение: Указать Python 3.11 или 3.12

### Способ 1: Через файл `runtime.txt` (рекомендуется)

1. **Создайте файл `runtime.txt` в корне проекта:**

```txt
python-3.11.9
```

Или:

```txt
python-3.12.7
```

2. **Добавьте файл в Git:**

```bash
cd dating-bot
git add runtime.txt
git commit -m "Add Python 3.11 runtime"
git push
```

3. **Railway автоматически пересоберет проект с указанной версией Python**

### Способ 2: Через настройки Railway

1. **В Railway Dashboard:**
   - Откройте ваш проект
   - Перейдите в **Settings** → **Deploy**
   - Найдите раздел **"Python Version"**
   - Выберите **Python 3.11** или **Python 3.12**
   - Сохраните изменения

2. **Railway пересоберет проект**

### Способ 3: Через переменную окружения

1. **В Railway Dashboard:**
   - Откройте ваш проект
   - Перейдите в **Variables**
   - Добавьте переменную:
     ```
     PYTHON_VERSION=3.11
     ```
   - Или:
     ```
     PYTHON_VERSION=3.12
     ```

2. **Railway пересоберет проект**

---

## 🚀 Быстрое решение

**Создайте файл `runtime.txt`:**

```bash
cd dating-bot
echo "python-3.11.9" > runtime.txt
git add runtime.txt
git commit -m "Fix: Use Python 3.11 for asyncpg compatibility"
git push
```

Railway автоматически пересоберет проект!

---

## 📝 Альтернатива: Удалить asyncpg (если не используется)

Если вы используете только MongoDB и не планируете использовать PostgreSQL:

1. **Удалите `asyncpg` из `requirements.txt`:**
   ```txt
   # Удалите эту строку:
   # asyncpg==0.29.0
   ```

2. **Сохраните и загрузите в GitHub:**
   ```bash
   git add requirements.txt
   git commit -m "Remove asyncpg (not used)"
   git push
   ```

**Но лучше использовать Python 3.11/3.12, чтобы сохранить совместимость!**

---

## ✅ Проверка

После применения решения:

1. Railway пересоберет проект
2. В логах должно быть:
   ```
   python   │   3.11.9   │   (или 3.12.7)
   ```
3. Сборка должна завершиться успешно
4. Бот запустится

---

## 📚 Дополнительная информация

- **Python 3.11** - стабильная версия, хорошо поддерживается
- **Python 3.12** - более новая, но тоже стабильная
- **Python 3.13** - слишком новая, многие пакеты еще не поддерживают

**Рекомендация:** Используйте Python 3.11 для максимальной совместимости.

---

**После применения решения Railway автоматически пересоберет проект! 🚀**

