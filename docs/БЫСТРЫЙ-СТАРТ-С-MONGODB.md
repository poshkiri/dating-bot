# ⚡ Быстрый старт с MongoDB

## ✅ Что уже сделано

1. **MongoDB интегрирована в код:**
   - ✅ Обновлен `database/connection.py` - добавлено подключение к MongoDB
   - ✅ Обновлен `middleware/database.py` - MongoDB доступна в обработчиках
   - ✅ Обновлен `main.py` - MongoDB инициализируется при запуске

2. **Созданы инструкции:**
   - ✅ `ЗАПУСК-НА-СЕРВЕРЕ.md` - как запустить бота на сервере
   - ✅ `ИНТЕГРАЦИЯ-MONGODB.md` - подробности интеграции

## 🚀 Что делать дальше

### Шаг 1: Проверьте .env файл

Убедитесь, что в `.env` указан правильный MongoDB URL:

```env
MONGODB_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DATABASE=dating_bot
```

**Важно:** Замените `<password>` на реальный пароль!

### Шаг 2: Запустите бота локально (для проверки)

```bash
python main.py
```

Вы должны увидеть:
```
✅ MongoDB подключена
INFO - Бот запущен
```

Если видите:
```
⚠️  MongoDB недоступна, используется SQLite
```

Это нормально - бот продолжит работать с SQLite.

### Шаг 3: Запустите бота на сервере

**Для работы 24/7 нужно запустить на сервере!**

См. подробную инструкцию: `ЗАПУСК-НА-СЕРВЕРЕ.md`

**Самый простой способ - Railway:**
1. Зарегистрируйтесь на https://railway.app
2. Создайте проект из GitHub
3. Добавьте переменные из `.env`
4. Готово! Бот работает 24/7

## 📋 Текущая ситуация

- ✅ MongoDB подключение работает
- ✅ Бот может использовать MongoDB (через `data["database"]`)
- ⚠️ Обработчики пока используют SQLite (для обратной совместимости)
- ✅ Можно постепенно переходить на MongoDB

## 🔧 Использование MongoDB в коде

В обработчиках теперь доступны оба варианта:

```python
@router.message(Command("start"))
async def cmd_start(message: Message, session: AsyncSession, database):
    # SQLite (как раньше)
    result = await session.execute(select(User).where(User.telegram_id == user_id))
    user = result.scalar_one_or_none()
    
    # ИЛИ MongoDB (новое)
    user = await database.users.find_one({"telegram_id": user_id})
```

## ⚠️ Важно

1. **Бот работает локально** - только когда ноутбук включен
2. **Для работы 24/7** - нужен сервер (Railway, Render, VPS)
3. **MongoDB Atlas** - уже настроена, нужно только запустить на сервере

## 📚 Дополнительная информация

- Полная инструкция по запуску на сервере: `ЗАПУСК-НА-СЕРВЕРЕ.md`
- Детали интеграции MongoDB: `ИНТЕГРАЦИЯ-MONGODB.md`
- Управление ботом: `УПРАВЛЕНИЕ-БОТОМ.md`

---

**Следующий шаг:** Запустите бота на сервере для работы 24/7! 🚀

