# 🌐 Запуск бота на сервере

Инструкция по запуску бота на сервере, чтобы он работал 24/7 даже при закрытом ноутбуке.

## ⚠️ Проблема

**Текущая ситуация:** Бот запущен локально на ноутбуке
- ❌ Бот работает только когда ноутбук включен
- ❌ При закрытии ноутбука бот останавливается
- ❌ Бот недоступен когда ноутбук выключен

**Решение:** Запустить бота на сервере, который работает 24/7

---

## 🚀 Вариант 1: Railway (Бесплатно, просто) ⭐ РЕКОМЕНДУЕТСЯ

### Преимущества:
- ✅ Бесплатный тариф (500 часов/месяц)
- ✅ Очень просто настроить
- ✅ Автоматический деплой из GitHub
- ✅ Автоматические бэкапы
- ✅ Работает сразу

### Шаги:

1. **Зарегистрируйтесь:** https://railway.app
   - Войдите через GitHub

2. **Настройте GitHub App (если репозитории не видны):**
   - На странице "Deploy a new GitHub repo" нажмите **"Configure GitHub App"**
   - Разрешите доступ к вашим репозиториям
   - Выберите репозиторий с ботом

3. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий с ботом
   
   **Если репозиторий не найден:**
   - Используйте "Empty Project" и загрузите код через Railway CLI
   - Или настройте GitHub App (см. выше)

4. **Настройте переменные окружения:**
   - В проекте нажмите на сервис
   - Перейдите в "Variables"
   - Добавьте все переменные из `.env`:
     ```
     BOT_TOKEN=ваш_токен
     MONGODB_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
     MONGODB_DATABASE=dating_bot
     ADMIN_USER_IDS=123456789
     # и т.д.
     ```
   
   **Важно:** Замените `<password>` в MONGODB_URL на реальный пароль!

5. **Настройте команду запуска:**
   - В настройках сервиса найдите "Settings"
   - В "Start Command" укажите: `python main.py`
   - В "Build Command" (опционально): `pip install -r requirements.txt`

6. **Готово!**
   - Railway автоматически запустит бота
   - Бот будет работать 24/7
   - Логи можно смотреть в разделе "Deployments"

### 📖 Подробная инструкция:

См. **[RAILWAY-РАЗВЕРТЫВАНИЕ.md](RAILWAY-РАЗВЕРТЫВАНИЕ.md)** для детальной пошаговой инструкции.

---

## 🚀 Вариант 2: Render (Бесплатно)

### Преимущества:
- ✅ Бесплатный тариф
- ✅ Простая настройка
- ✅ Автоматический деплой

### Шаги:

1. **Зарегистрируйтесь:** https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройте:**
   - **Name:** dating-bot
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`

4. **Добавьте переменные окружения:**
   - В разделе "Environment"
   - Добавьте все переменные из `.env`

5. **Готово!**
   - Render автоматически запустит бота

---

## 🖥️ Вариант 3: VPS (от $4/месяц)

### Популярные провайдеры:
- [DigitalOcean](https://www.digitalocean.com) - от $4/месяц
- [Vultr](https://www.vultr.com) - от $2.50/месяц
- [Hetzner](https://www.hetzner.com) - от €4/месяц
- [Timeweb Cloud](https://timeweb.cloud) - от 200₽/месяц (Россия)

### Настройка на VPS (Ubuntu):

#### Шаг 1: Подключитесь к серверу

```bash
ssh root@your-server-ip
```

#### Шаг 2: Обновите систему

```bash
apt update && apt upgrade -y
```

#### Шаг 3: Установите Python и зависимости

```bash
apt install python3 python3-pip git -y
```

#### Шаг 4: Загрузите код бота

**Вариант A: Через Git (если код в репозитории)**

```bash
git clone https://github.com/your-username/dating-bot.git
cd dating-bot
```

**Вариант B: Через SCP (если код локально)**

На вашем ноутбуке:
```bash
scp -r dating-bot root@your-server-ip:/root/
```

На сервере:
```bash
cd /root/dating-bot
```

#### Шаг 5: Установите зависимости

```bash
pip3 install -r requirements.txt
```

#### Шаг 6: Создайте .env файл

```bash
nano .env
```

Вставьте все переменные из вашего локального `.env` файла.

#### Шаг 7: Создайте systemd service

Создайте файл `/etc/systemd/system/dating-bot.service`:

```bash
nano /etc/systemd/system/dating-bot.service
```

Вставьте:

```ini
[Unit]
Description=Dating Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/dating-bot
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/python3 /root/dating-bot/main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `/root/dating-bot` на ваш реальный путь!

#### Шаг 8: Запустите сервис

```bash
# Перезагрузить systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable dating-bot

# Запустить сервис
systemctl start dating-bot

# Проверить статус
systemctl status dating-bot
```

#### Шаг 9: Проверьте логи

```bash
# Просмотр логов в реальном времени
journalctl -u dating-bot -f

# Последние 100 строк
journalctl -u dating-bot -n 100
```

---

## 🔧 Настройка MongoDB Atlas для сервера

### Важно: Network Access

1. Откройте MongoDB Atlas
2. Перейдите в **"Network Access"**
3. Добавьте IP адрес вашего сервера:
   - Для Railway/Render: используйте `0.0.0.0/0` (разрешить всем)
   - Для VPS: добавьте IP адрес вашего сервера

### Проверка подключения

На сервере выполните:

```bash
python3 -c "
import asyncio
from motor.motor_asyncio import AsyncIOMotorClient
from config import settings

async def test():
    client = AsyncIOMotorClient(settings.MONGODB_URL)
    await client.admin.command('ping')
    print('✅ MongoDB подключена!')
    client.close()

asyncio.run(test())
"
```

---

## ✅ Проверка работы

### 1. Проверьте статус бота:

**Railway/Render:**
- Откройте панель управления
- Проверьте логи деплоя

**VPS:**
```bash
systemctl status dating-bot
```

### 2. Проверьте в Telegram:

- Откройте Telegram
- Найдите вашего бота
- Отправьте `/start`
- Если бот отвечает - всё работает! ✅

### 3. Проверьте логи:

**VPS:**
```bash
journalctl -u dating-bot -f
```

**Railway/Render:**
- Смотрите логи в панели управления

---

## 🔄 Обновление бота на сервере

### Railway/Render:

Автоматически обновляется при push в GitHub.

Или вручную:
- Railway: нажмите "Redeploy"
- Render: нажмите "Manual Deploy"

### VPS:

```bash
# Остановить бота
systemctl stop dating-bot

# Обновить код
cd /root/dating-bot
git pull  # или загрузите новый код

# Установить зависимости
pip3 install -r requirements.txt

# Запустить бота
systemctl start dating-bot
```

---

## 🛑 Остановка бота

### Railway/Render:

- В панели управления нажмите "Stop"

### VPS:

```bash
systemctl stop dating-bot
```

---

## 📊 Мониторинг

### Railway:

- Логи доступны в разделе "Deployments"
- Метрики в разделе "Metrics"

### Render:

- Логи в разделе "Logs"
- Метрики в разделе "Metrics"

### VPS:

```bash
# Статус
systemctl status dating-bot

# Логи
journalctl -u dating-bot -f

# Использование ресурсов
top -p $(pgrep -f "python.*main.py")
```

---

## ❓ Решение проблем

### Бот не запускается

1. Проверьте логи
2. Проверьте переменные окружения
3. Проверьте подключение к MongoDB

### Ошибка подключения к MongoDB

1. Проверьте Network Access в MongoDB Atlas
2. Добавьте IP адрес сервера
3. Проверьте правильность connection string

### Бот падает

1. Проверьте логи на ошибки
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к файлам

---

## 💡 Рекомендации

1. **Используйте Railway** для начала (самый простой вариант)
2. **Настройте мониторинг** (UptimeRobot, Pingdom)
3. **Делайте бэкапы** регулярно
4. **Обновляйте код** через Git
5. **Следите за логами** первые дни

---

## 📚 Дополнительная информация

- Управление ботом: `УПРАВЛЕНИЕ-БОТОМ.md`
- Быстрые команды: `БЫСТРЫЕ-КОМАНДЫ.md`
- Основная документация: `../README.md`

---

**Успешного деплоя! 🚀**

