# 📤 Загрузка кода в GitHub

Инструкция по загрузке кода бота в GitHub для развертывания на Railway.

## 📋 Подготовка

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт
3. Нажмите **"+"** → **"New repository"**
4. Заполните:
   - **Repository name:** `dating-bot` (или любое имя)
   - **Description:** (опционально) "Telegram dating bot"
   - **Visibility:** Public или Private (на ваше усмотрение)
   - **НЕ** отмечайте "Initialize with README" (у нас уже есть код)
5. Нажмите **"Create repository"**

### Шаг 2: Скопируйте URL репозитория

После создания GitHub покажет URL репозитория, например:
```
https://github.com/ваш-username/dating-bot.git
```

Скопируйте этот URL - он понадобится позже.

---

## 🚀 Загрузка кода через Git Bash (MinGW)

### Шаг 1: Откройте Git Bash

1. Найдите **Git Bash** в меню Пуск
2. Или нажмите правой кнопкой в папке проекта → **"Git Bash Here"**

### Шаг 2: Перейдите в папку проекта

```bash
cd /c/Users/Светлана/Desktop/cursor/dating-bot
```

**Или если Git Bash открыт в папке проекта:**
```bash
pwd  # Проверьте текущую папку
```

### Шаг 3: Инициализируйте Git репозиторий

```bash
git init
```

### Шаг 4: Добавьте все файлы

```bash
git add .
```

### Шаг 5: Создайте первый коммит

```bash
git commit -m "Initial commit: Dating bot with MongoDB integration"
```

**Если видите ошибку про имя и email:**

```bash
# Настройте Git (замените на ваши данные)
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"

# Затем повторите коммит
git commit -m "Initial commit: Dating bot with MongoDB integration"
```

### Шаг 6: Добавьте удаленный репозиторий

```bash
# Замените URL на ваш реальный URL из GitHub
git remote add origin https://github.com/ваш-username/dating-bot.git
```

### Шаг 7: Загрузите код в GitHub

```bash
git branch -M main
git push -u origin main
```

**Если попросит авторизацию:**

- **GitHub больше не поддерживает пароли** для HTTPS
- Используйте **Personal Access Token**:
  1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
  2. Generate new token (classic)
  3. Выберите scope: `repo`
  4. Скопируйте токен
  5. Используйте токен вместо пароля при `git push`

**Или используйте SSH:**

```bash
# Измените remote на SSH
git remote set-url origin git@github.com:ваш-username/dating-bot.git

# Затем push
git push -u origin main
```

---

## ✅ Проверка

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. Должны быть видны: `main.py`, `config.py`, `requirements.txt` и т.д.

---

## 🔄 Обновление кода

После изменений в коде:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить в GitHub
git push
```

---

## 🚂 Теперь на Railway

После загрузки в GitHub:

1. Откройте Railway
2. Нажмите **"Configure GitHub App"**
3. Разрешите доступ к репозиториям
4. Выберите ваш репозиторий `dating-bot`
5. Railway автоматически найдет репозиторий

---

## 🔧 Альтернатива: Railway CLI (без GitHub)

Если не хотите использовать GitHub, можно загрузить напрямую:

### Шаг 1: Установите Railway CLI

```bash
# В Git Bash
npm install -g @railway/cli
```

**Если npm не установлен:**
- Установите Node.js: https://nodejs.org/
- Или используйте другой способ (см. ниже)

### Шаг 2: Войдите в Railway

```bash
railway login
```

Откроется браузер для авторизации.

### Шаг 3: Инициализируйте проект

```bash
cd /c/Users/Светлана/Desktop/cursor/dating-bot
railway init
```

### Шаг 4: Загрузите код

```bash
railway up
```

---

## ❓ Решение проблем

### Ошибка: "git: command not found"

**Решение:**
- Убедитесь, что Git установлен
- Перезапустите Git Bash
- Или используйте Railway CLI

### Ошибка: "Permission denied"

**Решение:**
- Проверьте правильность URL репозитория
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

### Ошибка: "Repository not found"

**Решение:**
- Проверьте, что репозиторий создан на GitHub
- Проверьте правильность URL
- Убедитесь, что вы авторизованы

---

## 📝 Быстрая команда (все в одном)

Если все готово, выполните в Git Bash:

```bash
cd /c/Users/Светлана/Desktop/cursor/dating-bot
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/dating-bot.git
git push -u origin main
```

**Замените `ВАШ-USERNAME` на ваш GitHub username!**

---

## 📚 Дополнительная информация

- Развертывание на Railway: `RAILWAY-РАЗВЕРТЫВАНИЕ.md`
- Запуск на сервере: `ЗАПУСК-НА-СЕРВЕРЕ.md`

---

**Успешной загрузки! 🚀**

