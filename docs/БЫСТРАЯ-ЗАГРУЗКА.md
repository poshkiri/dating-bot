# ⚡ Быстрая загрузка в GitHub

Краткая инструкция для Git Bash (MinGW).

## 🚀 Быстрый способ

### 1. Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Название: `dating-bot`
4. Нажмите **"Create repository"**
5. Скопируйте URL (например: `https://github.com/username/dating-bot.git`)

### 2. В Git Bash выполните:

```bash
# Перейдите в папку проекта
cd /c/Users/Светлана/Desktop/cursor/dating-bot

# Инициализируйте Git
git init

# Добавьте файлы
git add .

# Создайте коммит
git commit -m "Initial commit"

# Добавьте удаленный репозиторий (ЗАМЕНИТЕ URL!)
git remote add origin https://github.com/ВАШ-USERNAME/dating-bot.git

# Загрузите код
git branch -M main
git push -u origin main
```

### 3. Если попросит авторизацию:

**Используйте Personal Access Token:**
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Scope: `repo`
4. Используйте токен вместо пароля

### 4. На Railway:

1. Нажмите **"Configure GitHub App"**
2. Разрешите доступ
3. Выберите репозиторий `dating-bot`

---

## ✅ Готово!

Код загружен в GitHub, теперь можно развернуть на Railway!

---

**Подробная инструкция:** `ЗАГРУЗКА-В-GITHUB.md`

