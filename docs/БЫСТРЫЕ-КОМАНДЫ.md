# ⚡ Быстрые команды для управления ботом

Краткая шпаргалка с самыми часто используемыми командами.

## 🚀 Запуск

```bash
# Локально
python main.py

# В фоне (Linux/Mac)
nohup python3 main.py > bot.log 2>&1 &

# Через screen
screen -S bot
python3 main.py
# Ctrl+A, затем D для отсоединения

# Через systemd
sudo systemctl start dating-bot
```

## 🛑 Остановка

```bash
# В терминале
Ctrl+C

# Найти и убить процесс
pkill -f "python.*main.py"

# Через systemd
sudo systemctl stop dating-bot

# Через screen
screen -X -S bot quit
```

## 🔄 Перезагрузка

```bash
# Systemd
sudo systemctl restart dating-bot

# Вручную
pkill -f "python.*main.py" && python3 main.py &
```

## ✅ Проверка статуса

```bash
# Проверить процесс
ps aux | grep "python.*main.py"

# Systemd статус
sudo systemctl status dating-bot

# Проверить в Telegram
# Отправьте /start боту
```

## 📝 Логи

```bash
# Просмотр в реальном времени (systemd)
sudo journalctl -u dating-bot -f

# Последние 100 строк
sudo journalctl -u dating-bot -n 100

# Если через nohup
tail -f bot.log

# Поиск ошибок
sudo journalctl -u dating-bot | grep -i error
```

## 🔄 Обновление

```bash
# Остановить
sudo systemctl stop dating-bot

# Обновить код (Git)
git pull

# Установить зависимости
pip3 install -r requirements.txt

# Запустить
sudo systemctl start dating-bot
```

## 💾 Бэкап

```bash
# Бэкап базы данных
cp test_bot.db backup_$(date +%Y%m%d).db

# Бэкап .env
cp .env .env.backup
```

## 🔧 Автозапуск

```bash
# Включить автозапуск (systemd)
sudo systemctl enable dating-bot

# Проверить
sudo systemctl is-enabled dating-bot
```

## 📊 Мониторинг

```bash
# Использование ресурсов
top -p $(pgrep -f "python.*main.py")

# Размер базы данных
du -h test_bot.db
```

---

**Полная документация:** `УПРАВЛЕНИЕ-БОТОМ.md`

