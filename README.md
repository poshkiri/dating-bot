# 🤖 MeetUp - Telegram Dating Bot

Полнофункциональный Telegram-бот для знакомств с системой профилей, лайков, мероприятий и платежной интеграцией.

## ✨ Возможности

- 👤 **Система профилей** - создание анкет с фото и видео
- ❤️ **Лайки и симпатии** - система взаимных симпатий
- 🔍 **Умный поиск** - фильтры по возрасту, городу, полу
- 🎉 **Мероприятия** - создание и участие в событиях с геолокацией
- 💰 **Платежная система** - подписки и суперлайки через Telegram Payments и криптовалюту
- 👥 **Реферальная система** - приглашай друзей и получай бонусы
- 📊 **Статистика** - отслеживание активности и симпатий
- 🛡️ **Верификация** - проверка пользователей через фото
- 💬 **Поддержка** - анонимный чат с администраторами
- 👨‍💼 **Админ-панель** - управление пользователями, рассылки, модерация

## 🛠️ Технологии

- **Python 3.10+**
- **aiogram 3.x** - современный фреймворк для Telegram ботов
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL/SQLite** - основная база данных
- **MongoDB** - для хранения медиафайлов
- **Redis** - для FSM состояний (опционально)
- **Alembic** - миграции базы данных
- **Pydantic** - валидация настроек

## 📋 Требования

- Python 3.10 или выше
- PostgreSQL (или SQLite для разработки)
- MongoDB (опционально)
- Redis (опционально, для FSM)

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd dating-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `env.example` в `.env` и заполните необходимые параметры:

```bash
cp env.example .env
```

Отредактируйте `.env`:

```env
# Обязательные параметры
BOT_TOKEN=your_bot_token_here
BOT_USERNAME=meetup_dating_bot
ADMIN_USER_IDS=123456789

# База данных
DATABASE_URL=postgresql+asyncpg://user:password@localhost/dating_bot
# или для SQLite:
# DATABASE_URL=sqlite+aiosqlite:///./bot.db

MONGODB_URL=mongodb://localhost:27017
MONGODB_DATABASE=dating_bot

# Redis (опционально)
REDIS_URL=redis://localhost:6379/0

# Платежи
PAYMENT_PROVIDER_TOKEN=your_payment_provider_token

# Криптоплатежи (минимум одна сеть)
CRYPTO_WALLET_TRC20=TYourTronWalletAddressHere
CRYPTO_WALLET_BEP20=0xYourBSCWalletAddress
CRYPTO_WALLET_ERC20=0xYourEthereumWalletAddress

# Google Maps API (для геолокации)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Цены (в USD центах)
SUBSCRIPTION_PRICE=999  # $9.99
SUPER_LIKE_PRICE=199    # $1.99
```

### 5. Настройка базы данных

#### Для PostgreSQL:

```bash
# Создайте базу данных
createdb dating_bot

# Примените миграции
alembic upgrade head
```

#### Для SQLite:

Миграции применятся автоматически при первом запуске.

### 6. Запуск бота

```bash
python main.py
```

## 📁 Структура проекта

```
dating-bot/
├── alembic/              # Миграции базы данных
├── database/             # Модели и подключение к БД
│   ├── models.py         # SQLAlchemy модели
│   ├── connection.py     # Подключение к БД
│   └── redis_client.py   # Redis клиент
├── handlers/             # Обработчики сообщений
│   ├── commands.py       # Команды (/start, /help и т.д.)
│   ├── callbacks.py      # Обработка callback кнопок
│   ├── messages.py       # Обработка текстовых сообщений
│   ├── admin.py          # Админ-панель
│   ├── events.py         # Мероприятия
│   ├── verification.py   # Верификация пользователей
│   ├── payments.py       # Платежи
│   └── social.py         # Социальные сети
├── keyboards/            # Клавиатуры
│   └── common.py         # Общие клавиатуры
├── middleware/           # Middleware
│   └── database.py       # Dependency injection для БД
├── services/             # Сервисы
│   ├── telegram_payments.py  # Telegram Payments
│   ├── crypto_payments.py    # Криптоплатежи
│   └── google_maps.py        # Google Maps API
├── utils/                # Утилиты
│   ├── helpers.py        # Вспомогательные функции
│   └── locales.py       # Локализация
├── config.py             # Настройки приложения
├── main.py              # Точка входа
├── requirements.txt      # Зависимости
├── alembic.ini          # Конфигурация Alembic
└── env.example          # Пример .env файла
```

## 🔧 Основные команды

- `/start` - Начать работу с ботом
- `/help` - Руководство по использованию
- `/myprofile` - Просмотр своей анкеты
- `/stats` - Статистика пользователя
- `/events` - Мероприятия
- `/support` - Связь с поддержкой
- `/invite` - Пригласить друзей

## 💳 Платежная система

Бот поддерживает два типа платежей:

1. **Telegram Payments** - через провайдера (ЮKassa, Stripe и т.д.)
2. **Криптоплатежи** - USDT через сети:
   - TRC20 (Tron) - рекомендуется
   - BEP20 (Binance Smart Chain)
   - ERC20 (Ethereum)
   - Polygon

## 🗄️ База данных

Проект использует гибридный подход:
- **PostgreSQL/SQLite** - для основных данных (пользователи, лайки, события)
- **MongoDB** - для хранения медиафайлов (опционально)
- **Redis** - для FSM состояний (опционально, можно использовать MemoryStorage)

## 🔐 Безопасность

- Верификация пользователей через фото
- Система жалоб и модерации
- Админ-панель с правами доступа
- Валидация всех входных данных

## 📝 Миграции

Для применения миграций базы данных:

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1
```

## 🚢 Деплой

### Railway

Проект готов к деплою на Railway:

1. Подключите GitHub репозиторий
2. Установите переменные окружения в Railway
3. Railway автоматически определит Python и установит зависимости
4. Бот запустится автоматически

### Другие платформы

Бот можно развернуть на любой платформе, поддерживающей Python:
- Heroku
- DigitalOcean
- AWS
- VPS

## 📄 Лицензия

MIT License

## 👨‍💻 Разработка

Для разработки рекомендуется:

1. Использовать SQLite для локальной разработки
2. Настроить Redis для тестирования FSM
3. Использовать тестовый токен бота от @BotFather

## 🤝 Вклад

Приветствуются pull requests и issues!

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

---

**Сделано с ❤️ для знакомств в Telegram**

