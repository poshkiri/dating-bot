# Бот знакомств MeetUp ❤️

Полнофункциональный Telegram бот знакомств на aiogram 3.x с интеграцией платежей и криптоплатежей.

## 📋 Содержание

1. [Функционал](#функционал)
2. [Быстрая установка](#быстрая-установка)
3. [Настройка](#настройка)
4. [Запуск бота](#запуск-бота)
5. [Настройка MongoDB](#настройка-mongodb)
6. [Настройка Google Maps](#настройка-google-maps)
7. [Настройка криптоплатежей](#настройка-криптоплатежей)
8. [Запуск на сервере](#запуск-на-сервере)
9. [Структура проекта](#структура-проекта)
10. [Команды бота](#команды-бота)
11. [Решение проблем](#решение-проблем)
12. [Управление ботом](#управление-ботом)

---

## 🎯 Функционал

### Для пользователей:
- ✅ Создание и редактирование анкеты
- ✅ Система лайков/дизлайков
- ✅ Взаимные симпатии с показом username
- ✅ Фильтры поиска (город, пол, возраст)
- ✅ События и мероприятия
- ✅ Реферальная система
- ✅ Подписка ($9.99/месяц)
- ✅ Суперлайки ($1.99)
- ✅ Boost анкеты (поднятие в топ на 24ч)
- ✅ Верификация пользователей
- ✅ Система жалоб
- ✅ Статистика
- ✅ Поддержка (анонимный чат)
- ✅ Интеграция соцсетей (Instagram, VK)
- ✅ Многоязычность (Русский/English)

### Для администраторов:
- ✅ Админ-панель
- ✅ Рассылки с фото/видео и кнопками
- ✅ Модерация контента
- ✅ Статистика бота
- ✅ Управление пользователями
- ✅ Управление событиями

---

## 🚀 Быстрая установка

### 1. Установите зависимости:

```bash
pip install -r requirements.txt
```

### 2. Создайте файл `.env`:

Скопируйте `env.example` в `.env`:

```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

### 3. Заполните обязательные переменные в `.env`:

```env
# ОБЯЗАТЕЛЬНО:
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_USER_IDS=123456789

# Остальное можно настроить позже
```

### 4. Запустите бота:

```bash
python main.py
```

---

## ⚙️ Настройка

### Получение токена бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot` или `/mybots`
4. Следуйте инструкциям
5. Скопируйте полученный токен
6. Вставьте в `.env` файл: `BOT_TOKEN=ваш_токен`

### Получение Telegram ID (для админов)

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID
4. Добавьте в `.env`: `ADMIN_USER_IDS=ваш_id`

### Полный пример .env файла:

```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here
BOT_USERNAME=meetup_dating_bot

# Database
DATABASE_URL=sqlite+aiosqlite:///./test_bot.db
MONGODB_URL=mongodb://localhost:27017
MONGODB_DATABASE=dating_bot

# Redis (опционально)
REDIS_URL=redis://localhost:6379/0

# Payments
PAYMENT_PROVIDER_TOKEN=your_payment_provider_token

# Google Maps API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Crypto Payments - адреса кошельков
CRYPTO_WALLET_BEP20=0x1234567890abcdef1234567890abcdef12345678
CRYPTO_WALLET_TRC20=TYourTronWalletAddressHere123456789

# Admin
ADMIN_USER_IDS=123456789

# Pricing (в USD центах)
SUBSCRIPTION_PRICE=999
SUPER_LIKE_PRICE=199

# Limits
DAILY_LIKES_LIMIT=10
DAILY_DISLIKES_LIMIT=50
REFERRAL_BONUS_LIKES=5
```

---

## 🚀 Запуск бота

### Локально:

```bash
python main.py
```

### В фоновом режиме (Linux/Mac):

```bash
# Используя nohup
nohup python main.py > bot.log 2>&1 &

# Или используя screen
screen -S bot
python main.py
# Нажмите Ctrl+A, затем D для отсоединения

# Или используя tmux
tmux new -s bot
python main.py
# Нажмите Ctrl+B, затем D для отсоединения
```

### Что происходит при запуске:

1. **Подключение к базе данных** - создается/подключается SQLite база
2. **Проверка Redis** - если доступен, используется для хранения состояний
3. **Создание таблиц** - автоматически создаются таблицы в БД
4. **Регистрация обработчиков** - загружаются все обработчики команд и сообщений
5. **Запуск polling** - бот начинает получать обновления от Telegram

### Успешный запуск:

При успешном запуске вы увидите:

```
INFO - Бот запущен
INFO - Таблицы созданы
```

Бот готов к работе! Откройте Telegram и найдите вашего бота.

---

## 🗄️ Настройка MongoDB

### Вариант A: MongoDB Atlas (Облачный - Рекомендуется) ⭐

**Преимущества:**
- ✅ Бесплатный тариф (512 MB)
- ✅ Не нужно устанавливать
- ✅ Работает в облаке
- ✅ Автоматические бэкапы

**Шаги:**

1. **Зарегистрируйтесь:** https://www.mongodb.com/cloud/atlas
2. **Создайте кластер:**
   - Нажмите "Build a Database"
   - Выберите FREE (M0)
   - Выберите регион (ближайший к вам)
   - Создайте кластер (займет 3-5 минут)
3. **Создайте пользователя базы данных:**
   - Перейдите в "Database Access"
   - Нажмите "Add New Database User"
   - Username: `bot_user` (или любое имя)
   - Password: создайте надежный пароль (запомните!)
   - Права: "Read and write to any database"
   - Нажмите "Add User"
4. **Настройте Network Access:**
   - Перейдите в "Network Access"
   - Нажмите "Add IP Address"
   - Выберите "Allow Access from Anywhere" (0.0.0.0/0)
   - Нажмите "Confirm"
5. **Получите connection string:**
   - Нажмите "Connect" на вашем кластере
   - Выберите "Connect your application"
   - Выберите драйвер: Python, версия 3.7 or later
   - Скопируйте connection string
6. **Обновите .env файл:**
   ```env
   MONGODB_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   MONGODB_DATABASE=dating_bot
   ```
   
   **ВАЖНО:** Замените `<password>` на реальный пароль пользователя!

### Вариант B: Локальная установка MongoDB

#### Windows:

1. Скачайте MongoDB Community Server: https://www.mongodb.com/try/download/community
2. Запустите установщик
3. Выберите "Complete" установку
4. Отметьте "Install MongoDB as a Service"
5. После установки MongoDB запустится автоматически

#### Linux (Ubuntu/Debian):

```bash
# Импорт ключа
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# Добавление репозитория
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# Установка
sudo apt-get update
sudo apt-get install -y mongodb-org

# Запуск сервиса
sudo systemctl start mongod
sudo systemctl enable mongod
```

#### macOS:

```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

### Установка зависимостей Python:

```bash
pip install motor pymongo
```

Или установите все зависимости:

```bash
pip install -r requirements.txt
```

### Настройка .env для локального MongoDB:

```env
MONGODB_URL=mongodb://localhost:27017
MONGODB_DATABASE=dating_bot
```

**Примечание:** В текущей версии бот использует SQLite по умолчанию. Для перехода на MongoDB нужно обновить код (см. раздел "Миграция на MongoDB").

---

## 🗺️ Настройка Google Maps

### Шаг 1: Создайте аккаунт Google Cloud

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Войдите с вашим Google аккаунтом
3. Если у вас нет аккаунта, создайте его (это бесплатно)

### Шаг 2: Создайте проект

1. В верхней части страницы нажмите на выпадающий список проектов
2. Нажмите **"Создать проект"** (Create Project)
3. Введите название проекта
4. Нажмите **"Создать"**

### Шаг 3: Включите необходимые API

1. В меню слева выберите **"APIs & Services"** → **"Library"**
2. Найдите и включите:
   - **Geocoding API** (обязательно)
   - **Maps JavaScript API** (опционально)
3. Для каждого API нажмите **"Enable"**

### Шаг 4: Создайте API ключ

1. В меню слева выберите **"APIs & Services"** → **"Credentials"**
2. Нажмите **"+ CREATE CREDENTIALS"** → **"API key"**
3. Ключ будет создан автоматически
4. Скопируйте ключ

### Шаг 5: Ограничьте ключ (рекомендуется)

1. В списке ключей нажмите на созданный ключ
2. В разделе **"API restrictions"**:
   - Выберите **"Restrict key"**
   - Выберите только нужные API (Geocoding API)
3. Нажмите **"Save"**

### Шаг 6: Добавьте ключ в .env

```env
GOOGLE_MAPS_API_KEY=ваш_ключ_здесь
```

### Лимиты и стоимость:

- **Бесплатный лимит:** $200 кредитов в месяц
- **Geocoding API:** $5 за 1000 запросов
- Для начала бесплатного лимита более чем достаточно

---

## 💰 Настройка криптоплатежей

### Получение адресов кошельков

**Вариант 1: Использовать Bybit** (рекомендуется для начала)

1. Войдите в [Bybit](https://www.bybit.com/)
2. Перейдите в "Кошелек" → "USDT" → "Пополнить"
3. Скопируйте адрес для нужной сети (BEP20, TRC20 и т.д.)
4. Добавьте в `.env`

**Вариант 2: Создать отдельный кошелек**

- Trust Wallet, MetaMask, TronLink и т.д.

### Поддерживаемые сети:

- **BEP20** (BSC) - USDT, BUSD - низкие комиссии ⭐
- **TRC20** (Tron) - USDT - низкие комиссии ⭐
- **ERC20** (Ethereum) - USDT, USDC - высокие комиссии
- **Polygon** - USDT, USDC - низкие комиссии

### Настройка .env:

```env
# Минимум одна сеть (рекомендуется BEP20 или TRC20)
CRYPTO_WALLET_BEP20=0x1234567890abcdef1234567890abcdef12345678
CRYPTO_WALLET_TRC20=TYourTronWalletAddressHere123456789
CRYPTO_WALLET_ERC20=0x...  # Опционально
CRYPTO_WALLET_POLYGON=0x...  # Опционально
```

### Безопасность:

- ✅ Адреса кошельков можно публиковать (они для получения средств)
- ❌ Никогда не публикуйте приватные ключи!
- ✅ Регулярно выводите средства с биржи
- ✅ Используйте 2FA в Bybit

---

## 🌐 Запуск на сервере

### ⚠️ Важно: Почему бот не работает при закрытом ноутбуке

**Проблема:** Бот запущен локально на вашем ноутбуке. Когда ноутбук закрыт или выключен, бот не работает.

**Решение:** Нужно запустить бота на сервере, который работает 24/7.

### Вариант 1: Railway (Бесплатно, просто) ⭐

1. Зарегистрируйтесь: https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий (или загрузите код)
4. Добавьте переменные окружения из `.env`
5. Railway автоматически запустит бота

### Вариант 2: Render (Бесплатно)

1. Зарегистрируйтесь: https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения
5. Запустите бота

### Вариант 3: VPS (от $4/месяц)

**Популярные провайдеры:**
- [DigitalOcean](https://www.digitalocean.com) - от $4/месяц
- [Vultr](https://www.vultr.com) - от $2.50/месяц
- [Hetzner](https://www.hetzner.com) - от €4/месяц

**Настройка на VPS (Ubuntu):**

```bash
# 1. Подключитесь к серверу
ssh root@your-server-ip

# 2. Обновите систему
apt update && apt upgrade -y

# 3. Установите Python
apt install python3 python3-pip git -y

# 4. Загрузите код бота
git clone your-repo-url
cd dating-bot

# 5. Установите зависимости
pip3 install -r requirements.txt

# 6. Создайте .env файл
nano .env
# Вставьте настройки

# 7. Запустите через screen
screen -S bot
python3 main.py
# Нажмите Ctrl+A, затем D для отсоединения
```

**Или создайте systemd service для автозапуска:**

Создайте файл `/etc/systemd/system/dating-bot.service`:

```ini
[Unit]
Description=Dating Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/dating-bot
ExecStart=/usr/bin/python3 /root/dating-bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl enable dating-bot
sudo systemctl start dating-bot
sudo systemctl status dating-bot
```

---

## 📁 Структура проекта

```
dating-bot/
├── alembic/              # Миграции базы данных
├── database/             # Модели и подключение к БД
│   ├── models.py        # SQLAlchemy модели
│   ├── connection.py    # Подключение к БД
│   └── redis_client.py  # Подключение к Redis
├── handlers/            # Обработчики
│   ├── commands.py      # Команды бота
│   ├── callbacks.py     # Callback обработчики
│   ├── messages.py      # Обработка сообщений
│   ├── admin.py         # Админ-панель
│   ├── payments.py       # Обработка платежей
│   ├── events.py         # События
│   ├── verification.py   # Верификация
│   └── social.py         # Соцсети
├── keyboards/           # Клавиатуры
├── services/            # Сервисы
│   ├── crypto_payments.py  # Криптоплатежи
│   ├── telegram_payments.py # Telegram Payments
│   └── google_maps.py     # Google Maps
├── utils/               # Утилиты
│   ├── helpers.py       # Вспомогательные функции
│   └── locales.py       # Локализация
├── middleware/          # Middleware
│   └── database.py      # Database middleware
├── config.py            # Конфигурация
├── main.py              # Точка входа
├── requirements.txt     # Зависимости
├── env.example          # Пример .env файла
└── README.md            # Документация
```

---

## 📱 Команды бота

### Для пользователей:
- `/start` - Начать работу
- `/myprofile` - Моя анкета
- `/complaint` - Пожаловаться
- `/language` - Сменить язык
- `/help` - Руководство
- `/events` - Мероприятия
- `/stats` - Статистика
- `/support` - Поддержка
- `/invite` - Пригласить друзей

### Для администраторов:
- `/admin` - Админ-панель

---

## ⚙️ Настройки

### Лимиты (в .env):

```env
DAILY_LIKES_LIMIT=10          # Дневной лимит лайков
DAILY_DISLIKES_LIMIT=50       # Дневной лимит дизлайков
REFERRAL_BONUS_LIKES=5        # Бонус за реферала
```

### Цены (в USD центах):

```env
SUBSCRIPTION_PRICE=999        # $9.99/месяц
SUPER_LIKE_PRICE=199         # $1.99
```

### Подписка:

- Цена: $9.99/месяц (настраивается в .env)
- Дает неограниченные лайки
- Приоритет в показе анкет

### Суперлайки:

- Цена: $1.99 (настраивается в .env)
- Позволяет отправить сообщение или видео
- Привлекает больше внимания

---

## ❌ Решение проблем

### Ошибка: "BOT_TOKEN is required"

**Решение:**
- Проверьте, что файл `.env` существует
- Убедитесь, что `BOT_TOKEN` заполнен в `.env`
- Проверьте, что нет лишних пробелов: `BOT_TOKEN=токен` (без пробелов вокруг `=`)

### Ошибка: "ModuleNotFoundError"

**Решение:**
```bash
pip install -r requirements.txt
```

### Ошибка: "Invalid token"

**Решение:**
- Проверьте правильность токена от BotFather
- Убедитесь, что скопировали весь токен
- Попробуйте создать нового бота через BotFather

### Ошибка подключения к базе данных

**Решение:**
- SQLite создастся автоматически
- Проверьте права на запись в папку проекта
- Убедитесь, что файл `test_bot.db` не заблокирован другим процессом

### Бот не отвечает

**Решение:**
1. Проверьте, что бот запущен (нет ошибок в консоли)
2. Убедитесь, что используете правильного бота в Telegram
3. Отправьте команду `/start` боту
4. Проверьте логи на наличие ошибок

### Ошибка MongoDB: "bad auth : authentication failed"

**Решение:**
1. Проверьте, что в connection string заменен `<password>` на реальный пароль
2. Убедитесь, что пользователь создан в MongoDB Atlas
3. Проверьте правильность username и password
4. Убедитесь, что Network Access настроен (0.0.0.0/0 или ваш IP)

### Бот не работает при закрытом ноутбуке

**Решение:**
- Запустите бота на сервере (VPS, Railway, Render)
- См. раздел "Запуск на сервере"

---

## 🔧 Дополнительные настройки

### Настройка Redis (опционально)

Redis используется для хранения состояний FSM. Если Redis не установлен, используется MemoryStorage (данные теряются при перезапуске).

**Установка Redis (Linux):**
```bash
sudo apt install redis-server
sudo systemctl start redis
```

**Установка Redis (Windows):**
- Используйте WSL2 или Docker
- Или оставьте без Redis (будет использован MemoryStorage)

### Логирование

Бот выводит логи в консоль. Для сохранения логов в файл добавьте в `main.py`:

```python
import logging
file_handler = logging.FileHandler('bot.log')
file_handler.setLevel(logging.INFO)
logger.addHandler(file_handler)
```

### Миграция на MongoDB

В текущей версии бот использует SQLite по умолчанию. Для перехода на MongoDB нужно:

1. Настроить MongoDB (см. раздел "Настройка MongoDB")
2. Обновить `database/connection.py` для использования MongoDB
3. Обновить обработчики для работы с MongoDB вместо SQLAlchemy

---

## 📝 Разработка

Для разработки рекомендуется использовать виртуальное окружение:

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

---

## 🎮 Управление ботом

Подробные инструкции по управлению ботом находятся в папке `docs/`:

- **[УПРАВЛЕНИЕ-БОТОМ.md](docs/УПРАВЛЕНИЕ-БОТОМ.md)** - Полное руководство:
  - Запуск, остановка, перезагрузка бота
  - Проверка статуса
  - Просмотр логов
  - Автозапуск при перезагрузке
  - Обновление кода
  - Резервное копирование
  - Мониторинг

- **[БЫСТРЫЕ-КОМАНДЫ.md](docs/БЫСТРЫЕ-КОМАНДЫ.md)** - Краткая шпаргалка с командами

- **[ЗАПУСК-НА-СЕРВЕРЕ.md](docs/ЗАПУСК-НА-СЕРВЕРЕ.md)** - Как запустить бота на сервере для работы 24/7:
  - Railway (бесплатно, просто) ⭐
  - Render (бесплатно)
  - VPS (от $4/месяц)

- **[ИНТЕГРАЦИЯ-MONGODB.md](docs/ИНТЕГРАЦИЯ-MONGODB.md)** - Детали интеграции MongoDB в код

- **[БЫСТРЫЙ-СТАРТ-С-MONGODB.md](docs/БЫСТРЫЙ-СТАРТ-С-MONGODB.md)** - Быстрый старт с MongoDB

### Быстрые команды:

```bash
# Запуск
python main.py

# Остановка
Ctrl+C

# Перезагрузка (systemd)
sudo systemctl restart dating-bot

# Проверка статуса
sudo systemctl status dating-bot

# Просмотр логов
sudo journalctl -u dating-bot -f
```

## 📚 Дополнительная информация

- **Aiogram документация:** https://docs.aiogram.dev/
- **MongoDB документация:** https://docs.mongodb.com/
- **Google Maps API:** https://developers.google.com/maps/documentation

---

## 🛑 Остановка бота

Нажмите `Ctrl+C` в консоли, где запущен бот.

---

## 📄 Лицензия

MIT

---

## 💬 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем"
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настроек в `.env`
4. Проверьте логи бота на наличие ошибок

---

**Удачной разработки! 🚀**
